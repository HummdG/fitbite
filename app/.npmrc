# Expo SDK 56 pulls react-dom@19.2.7 (web/radix peers via expo-router) which wants
# react@^19.2.7, but the SDK pins react@19.2.3. We target native, not web, so this
# peer mismatch is harmless. legacy-peer-deps lets npm install proceed.
legacy-peer-deps=true
